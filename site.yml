---
- name: Common baseline for all hosts
  hosts: all
  become: true
  gather_facts: true
  roles:
    - common

- name: Configure Proxmox nodes
  hosts: proxmox
  become: true
  gather_facts: true
  roles:
    - proxmox

- name: Configure generic Linux servers
  hosts: linux_server
  become: true
  gather_facts: true
  roles:
    - linux_server

- name: Configure generic Linux servers
  hosts: linux_server:!lxc_container
  become: true
  gather_facts: true
  roles:
    - linux_server_no_lxc

- name: Ensure NetBird on all clients
  hosts: all
  become: true
  gather_facts: true
  roles:
    - netbird
